// 다음 C언어 프로그램에서 !a && !b 부분과 동일한 의미를 가지는 것은 어떤 것인가?

#include( ) {
  int a, b;
  for (a = 0; a < 2; a++)
    for (b = 0; b < 2; b++)
      printf("%%d", !a && !b);
}

// 답 :    !a && !b = !(a || b) 
